name: macOS

on: [push, pull_request]

jobs:
  macos-build:

    runs-on: macos-latest

    steps:    
    - uses: actions/checkout@v2
    - name: Prepare to compile
      run: brew install sdl2 gcc
    - name: Configure to compile
      run: mkdir Release && cd Release && cmake -D CMAKE_BUILD_TYPE=Release -D CMAKE_C_COMPILER=clang -D CMAKE_CXX_COMPILER=g++-10 ..
    - name: Compilation
      run: cd Release && make -j $(sysctl -n hw.ncpu)
    - name: Prepare to upload
      run: mkdir ready && cp Release/libGOESP.dylib ready/ && rm -rf Release
    - name: SHA1
      working-directory: ready
      run: shasum libGOESP.dylib > libGOESP.dylib.sha1
      
    # - name: Configure to compile AVX
    #   run: mkdir Release && cd Release && cmake -D CMAKE_BUILD_TYPE=Release-D CMAKE_CXX_FLAGS=-mavx -D CMAKE_C_COMPILER=clang -D CMAKE_CXX_COMPILER=g++-10 ..
    # - name: Compilation AVX
    #   run: cd Release && make -j $(sysctl -n hw.ncpu)
    # - name: Prepare to upload AVX
    #   run: cp Release/libGOESP.dylib ready/libGOESP_AVX.dylib && rm -rf Release
    # - name: SHA1 AVX
    #   working-directory: ready
    #   run: shasum libGOESP_AVX.dylib > libGOESP_AVX.dylib.sha1
    #   
    #   
    # - name: Configure to compile AVX2
    #   run: mkdir Release && cd Release && cmake -D CMAKE_BUILD_TYPE=Release-D CMAKE_CXX_FLAGS=-mavx2 -D CMAKE_C_COMPILER=clang -D CMAKE_CXX_COMPILER=g++-10 ..
    # - name: Compilation AVX2
    #   run: cd Release && make -j $(sysctl -n hw.ncpu)
    # - name: Prepare to upload AVX2
    #   run: cp Release/libGOESP.dylib ready/libGOESP_AVX2.dylib && rm -rf Release
    # - name: SHA1 AVX2
    #   working-directory: ready
    #   run: shasum libGOESP_AVX2.dylib > libGOESP_AVX2.dylib.sha1

    - name: Push to Discord
      run: |
            cd ready
            tar -czvf "../GOESP Release macOS.tar.gz" *
            cd ..
            curl -X POST -H 'Content-Type: multipart/form-data' -F 'file=@GOESP Release macOS.tar.gz' -F (('payload_json={\"embeds\": [{\"color\": 7506394,\"fields\": [{\"name\": \"GitHub repo\",\"value\": \"[Click!](https://github.com/playday3008/GOESP)\"},{\"name\": \"Size\",\"value\": \"x.xx MB\"}],\"author\": {\"name\": \"GOESP (macOS)\"},\"footer\": {\"text\": \"ID: xxxxxxxx\"},\"timestamp\": \"yyyy-MM-ddTHH:mm:ss.000Z\"}]}') -replace "yyyy-mm-ddTHH:mm:ss", (Get-Date -Format "yyyy-MM-ddTHH:mm:ss") -replace "x.xx MB", ("{0:N2} MB" -f ((Get-Item 'GOESP Release macOS.tar.gz').length/1MB)) -replace "xxxxxxxx", ${{github.run_id}}) ${{ secrets.DISCORD_HOOK }}
      shell: pwsh
      
    - uses: actions/upload-artifact@v1
      with:
        name: GOESP Release macOS
        path: ready
